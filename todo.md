# Pi
- [x] List number of tasks at the bottom
- [x] Only include incomplete tasks in system reminders. (The rest are marked as 'complete')
- [ ] edit diff context should include function name lines, etc like neovim scrolling. hide middle if nested too deeply
- [x] Good reason to use a subagent: if you're stuck in a loop trying to do something
- [x] Good reason to use a subagent: you're not making any progress debugging a problem
